-- Seed script: Run AFTER creating your first admin user via Supabase Auth
-- Replace 'your-admin-user-uuid' with the actual UUID from auth.users

-- Example: Promote first user to admin
-- UPDATE profiles SET role = 'admin' WHERE email = 'admin@vsgameology.com';
